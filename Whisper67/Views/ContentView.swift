import SwiftUI

/// Legacy entry kept for previews; main UI is SettingsView / HomeDashboardView.
struct ContentView: View {
    var body: some View {
        SettingsView()
    }
}

#Preview {
    ContentView()
}
